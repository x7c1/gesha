schemas:
  PetContainer:
    type: object
    required:
      - pet
    properties:
      pet:
        $ref: '#/components/schemas/Pet'
  Pet:
    type: object
    required:
      - id
      - name
    properties:
      id:
        type: integer
      name:
        type: string
