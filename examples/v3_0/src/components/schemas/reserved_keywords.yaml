schemas:
  ReservedKeywords:
    type: object
    required:
      - break
      - continue
      - move
      - ref
      - type
    properties:
      break:
        type: string
      continue:
        type: string
      move:
        type: string
      ref:
        type: string
      type:
        type: string
