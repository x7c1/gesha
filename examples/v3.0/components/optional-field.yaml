schemas:
  Pet:
    type: object
    required:
      - id
    properties:
      id:
        type: integer
      tag:
        type: string

  OptionalRef:
    type: object
    properties:
      pet:
        $ref: '#/components/schemas/Pet'
