pub mod schemas {
    pub struct Numeric {
        pub integer_without_format: i64,
        pub int1: i32,
        pub int2: i64,
        pub number_without_format: f64,
        pub num1: f32,
        pub num2: f64,
    }
}
